/**
 * Layout primitives — Mekari Jurnal app shell.
 *
 * These mirror Pixel 3's expected sidebar/navbar geometry and feed into the
 * `usePixelLayout()` composable. Keep them in sync with `docs/design.md`.
 */

export const NAVBAR_HEIGHT = 56;
export const SIDEBAR_COLLAPSED_WIDTH = 56;
export const SIDEBAR_EXPANDED_WIDTH = 216;
export const SIDEBAR_BOTTOM_BAR_HEIGHT = 68;

/** Submenu / sub-rail width (Figma node 1029:3771). */
export const SIDEBAR_CHILD_WIDTH = 208;

/** PageTitle band height (Figma node 1:17750 → PageTitle frame). */
export const PAGE_TITLE_HEIGHT = 72;
